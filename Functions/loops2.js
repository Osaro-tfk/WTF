// while
var start = 1
var limit = 12
while (start <= limit) {
    document.writeln("<h1>2 x " + start +  " = " + (2 * start) + "</h1>" )
    start = start + 1
}

// do while
// counter = 4
// do{
//     console.log("run")
// }while (counter <= 2)

// for loop
for(start = 1; start<= limit; start = start + 1){
    document.writeln("<h1>2 x " + start +  " = " + (2 * start) + "</h1>" )
}